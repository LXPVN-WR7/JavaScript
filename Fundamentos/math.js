const raio = 5.6;

// Utilizando o objeto Math e acessando a função (pow) para calcular a potência
// Utilizando também a constante do objeto Math (PI)
const area = Math.PI * Math.pow(raio, 2);

// Acessando o valor de (PI) no objeto Math
console.log(Math.PI);

console.log(area);

// Exibindo o typeof de Math (Objeto)
console.log(typeof Math);